# Correlation

def run_gnns(config):
    # Extract params from yaml config file
    param1 = config["param1"]
    param2 = config["param2"]
    param3 = config["param3"]

    print(f"Running run_gnns with {param1} param1, {param2} param2, {param3} param3")
    # Logic would go here...
